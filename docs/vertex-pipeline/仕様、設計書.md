# Vertex AI Pipeline 導入設計書
## study-gcp-mlops への Vertex AI Pipeline 統合

---

## 1. 目的

```text
既存の Cloud Run Job（力技スクリプト）を段階的に Vertex AI Pipeline に移行する

・RandomForest 学習後の評価・デプロイ判断を Pipeline として実装する
・品質ゲートを仕組みとして組み込む
・Champion/Challenger パターンを実装し、モデル切り替えを自動化する
・Vertex AI Pipeline の概念と実装を一致させる土台を作る
```

---

## 2. 既存構成との対応

### 現状（Cloud Run Job）

```text
Cloud Run Job（batch）1スクリプト
  ├── California Housing データ取得
  ├── train/test 分割
  ├── RandomForest 学習
  ├── 評価（RMSE, MAE）→ MLflow 記録
  ├── モデル保存 → GCS
  └── メトリクス投入 → BigQuery
```

### 目指す姿（Vertex AI Pipeline）

```text
Vertex AI Pipeline
  ├── Step1: データ取得・分割       ← Cloud Run Job から切り出し
  ├── Step2: 学習                  ← Cloud Run Job から切り出し
  ├── Step3: 評価（RMSE/MAE算出）  ← Cloud Run Job から切り出し
  ├── Step4: 品質ゲート            ← 新規追加（閾値チェック）
  ├── Step5: Champion/Challenger 比較 ← 新規追加
  └── Step6: デプロイ判断・実行    ← 新規追加
```

### 既存コンポーネントの扱い

| コンポーネント | 扱い | 理由 |
|---|---|---|
| BigQuery（metricsテーブル） | そのまま活用 | Champion モデル取得に流用 |
| GCS（models/） | そのまま活用 | モデルアーティファクト保存先 |
| Cloud Run Service（推論API） | そのまま | Pipeline の対象外 |
| Cloud Scheduler | Pipeline トリガーに置き換え | Vertex AI Pipeline 定期実行 |
| Discord 通知 | Pipeline 完了イベントに接続 | 通知ロジックは再利用 |
| MLflow | Vertex AI Experiments と併用 | 移行コストを考慮し段階的に |

---

## 3. 導入スコープ（フェーズ設計）

### 導入方針

```text
既存の学習フローを壊さずに
評価・デプロイ判断部分だけを先に Pipeline 化する

理由：
・リスクが低い（学習処理を触らない）
・チームの成果として最も価値が高い部分（品質ゲート）を先に実現する
・Pipeline の概念理解には評価・デプロイ判断部分で十分
```

### Phase 1（本設計書のスコープ）

```text
既存 Cloud Run Job は触らない
学習済みモデルを GCS から受け取り
評価・Champion 比較・デプロイ判断を Pipeline で実装する

Pipeline への入力
  └── GCS上の学習済みモデルパス

Pipeline の処理
  ├── Step1: モデル評価（RMSE/MAE算出）
  ├── Step2: 品質ゲート（閾値チェック）
  ├── Step3: Champion/Challenger 比較
  └── Step4: デプロイ判断

Pipeline の出力
  └── Vertex AI Endpoint へのデプロイ or スキップ
```

### Phase 2（将来）

```text
学習部分も Pipeline に統合
全体を Vertex AI Pipelines に移行
```

---

## 4. アーキテクチャ

### Pipeline 全体フロー

```text
[Cloud Run Job（既存・変更なし）]
  └── 学習 → モデル保存 → GCS models/

         ↓ モデルパスを渡す

[Vertex AI Pipeline（新規）]
  ├── Step1: evaluate_model
  │     ├── GCS からモデルロード
  │     ├── California Housing テストデータで評価
  │     ├── RMSE / MAE 算出
  │     └── Vertex AI Experiments にメトリクス記録
  │
  ├── Step2: quality_gate
  │     ├── RMSE が閾値以下か判定
  │     ├── 合格 → Step3 へ進む
  │     └── 不合格 → Pipeline 終了 + Discord 通知
  │
  ├── Step3: compare_champion
  │     ├── BigQuery から Champion モデルパス取得
  │     ├── Champion と Challenger の RMSE 比較
  │     ├── Challenger が優位（-X% 以上改善）→ Step4 へ
  │     └── 改善なし → Pipeline 終了 + Discord 通知
  │
  └── Step4: deploy_model
        ├── Vertex AI Endpoint にデプロイ
        ├── トラフィック切り替え
        └── BigQuery に Champion 更新 + Discord 通知

[Vertex AI Endpoint]
  └── Cloud Run Service（推論 API）から呼び出し
```

---

## 5. ディレクトリ構成（追加分）

```text
study-gcp-mlops/
├── src/
│   ├── batch/              # 既存（変更なし）
│   ├── api/                # 既存（変更なし）
│   └── pipeline/           # 新規追加
│       ├── components/
│       │   ├── evaluate_model.py     # Step1: モデル評価
│       │   ├── quality_gate.py       # Step2: 品質ゲート
│       │   ├── compare_champion.py   # Step3: Champion 比較
│       │   └── deploy_model.py       # Step4: デプロイ
│       ├── pipeline.py               # Pipeline 定義（DAG）
│       └── requirements.txt          # Pipeline 用依存関係
├── terraform/
│   └── pipeline.tf         # 新規追加（Vertex AI リソース）
└── .github/workflows/
    └── pipeline.yml        # 新規追加（Pipeline CI/CD）
```

---

## 6. 各 Step の詳細設計

### Step1: evaluate_model

```python
# 責務
# - GCS からモデルをロード
# - テストデータで RMSE / MAE を算出
# - Vertex AI Experiments にメトリクスを記録
# - 次 Step にメトリクスを渡す

# 入力
model_gcs_path: str   # GCS上のモデルパス

# 出力
rmse: float
mae: float
model_gcs_path: str   # 次 Step に引き継ぐ
```

### Step2: quality_gate

```python
# 責務
# - RMSE が閾値以下かを判定
# - 不合格の場合は Pipeline を終了する
# - 閾値は環境変数 or Pipeline パラメータで外部化

# 入力
rmse: float
rmse_threshold: float  # デフォルト: 0.8（調整可能）

# 出力
is_passed: bool

# 品質ゲート不合格時の動作
# - Pipeline を終了（後続 Step を実行しない）
# - Discord に通知（モデルパス・RMSE・閾値を含む）
```

### Step3: compare_champion

```python
# 責務
# - BigQuery から現 Champion モデルのメトリクスを取得
# - Challenger（新モデル）と RMSE を比較
# - 有意な改善がある場合のみ次 Step に進む

# 入力
challenger_rmse: float
challenger_model_path: str

# BigQuery クエリ（既存テーブル活用）
# SELECT model_path, rmse
# FROM mlops.metrics
# ORDER BY rmse ASC LIMIT 1

# 出力
should_deploy: bool
champion_rmse: float

# 比較基準（設計上の重要な決定事項）
# 単純比較ではなく「有意な改善」を定義する
# 例: challenger_rmse < champion_rmse * 0.99
#     （1%以上の改善がある場合のみデプロイ）
# 理由: モデルのランダム性によるブレを除外するため
```

### Step4: deploy_model

```python
# 責務
# - Vertex AI Endpoint にモデルをデプロイ
# - BigQuery の Champion レコードを更新
# - Discord に成功通知を送信

# 入力
model_gcs_path: str
endpoint_id: str

# デプロイ戦略
# トラフィック 100% 切り替え（Phase 1）
# ※ Phase 2 で 10% → 100% の段階的切り替えに拡張予定

# 出力
deployed_model_id: str
endpoint_url: str
```

---

## 7. 品質ゲートの設計思想

### なぜ単純な精度比較ではいけないか

```text
問題: モデルのランダム性によるブレ

RandomForest は学習のたびに微妙に結果が変わる
  └── random_state を固定しても環境差異で変わりうる

素朴な実装（NG）
  └── 「新モデルの RMSE < Champion の RMSE ならデプロイ」
       └── ブレの範囲内の差異で誤判定する

正しい実装
  └── 「新モデルが Champion より有意に良い場合のみデプロイ」
       └── 例: 1% 以上の改善を条件とする
```

### 閾値の外部化

```text
品質ゲートの閾値はハードコードしない
  └── Pipeline 実行パラメータとして外部化
       └── 実験・チューニングが容易になる
       └── 業務環境への応用時に再利用しやすい
```

---

## 8. Terraform 追加リソース

```hcl
# pipeline.tf に追加するリソース

# Vertex AI Endpoint
resource "google_vertex_ai_endpoint" "mlops_endpoint" {
  name         = "california-housing-endpoint"
  display_name = "California Housing Endpoint"
  location     = var.region
}

# Vertex AI Experiments（メトリクス記録）
# ※ Terraform 非対応のため gcloud CLI or SDK で作成

# Pipeline 実行用 Service Account
resource "google_service_account" "pipeline_sa" {
  account_id   = "vertex-pipeline-sa"
  display_name = "Vertex AI Pipeline Service Account"
}

# 必要な IAM ロール
resource "google_project_iam_member" "pipeline_sa_roles" {
  for_each = toset([
    "roles/aiplatform.user",
    "roles/storage.objectAdmin",
    "roles/bigquery.dataEditor",
  ])
  role   = each.value
  member = "serviceAccount:${google_service_account.pipeline_sa.email}"
}
```

---

## 9. CI/CD 設計（pipeline.yml）

```yaml
# .github/workflows/pipeline.yml

# トリガー
# - src/pipeline/ 配下の変更が main にマージされた時

# ジョブ構成
jobs:
  test:
    # Pipeline コンポーネントの単体テスト

  build-and-push:
    # 各コンポーネントのコンテナをビルド
    # Artifact Registry にプッシュ

  compile-pipeline:
    # pipeline.py をコンパイル → pipeline.json 生成

  run-pipeline:
    # Vertex AI Pipeline を実行
    # 実行結果を Discord に通知
```

---

## 10. 学習ポイント整理

### Vertex AI Pipeline を使うことで理解できる概念

| 概念 | Cloud Run Job との違い | 業務への応用 |
|---|---|---|
| DAG（有向非巡回グラフ） | スクリプトは逐次実行のみ | Pipeline は依存関係で並列化可能 |
| コンポーネント分離 | 1スクリプトに全処理 | Step 単位で再実行・差し替え可能 |
| アーティファクト管理 | 手動で GCS パスを管理 | Pipeline が自動でバージョン管理 |
| 品質ゲート | コードに埋め込み | Step として明示的に設計 |
| メタデータ記録 | MLflow で手動 | Vertex AI Experiments と統合 |

### Champion/Challenger を実装することで理解できること

```text
1. ベースライン（Champion）の重要性
   └── 比較対象がないと改善を測れない

2. 有意差の定義
   └── 単純比較では不十分
   └── ブレを考慮した閾値設計が必要

3. デプロイ判断の自動化
   └── 人間の勘ではなく指標で判断する

4. 将来の Vertex AI Endpoint トラフィック分割への理解
   └── Phase 2: 10% → 100% 段階的切り替えの土台
```

---

## 11. 実装スケジュール（目安）

```text
Day1: 環境準備
  └── Vertex AI API 有効化
  └── Service Account / IAM 設定（Terraform）
  └── Vertex AI Endpoint 作成（Terraform）

Day2: Step1 / Step2 実装
  └── evaluate_model.py
  └── quality_gate.py
  └── ローカルで単体動作確認

Day3: Step3 / Step4 実装
  └── compare_champion.py（BigQuery 連携）
  └── deploy_model.py（Vertex AI Endpoint 連携）

Day4: Pipeline 統合
  └── pipeline.py（DAG 定義）
  └── Vertex AI Pipeline 実行確認

Day5: CI/CD 整備
  └── pipeline.yml 追加
  └── Discord 通知接続
  └── 動作確認・ドキュメント整備
```

---

## 12. 参考リソース

```text
Vertex AI Pipelines（KFP SDK v2）
  https://cloud.google.com/vertex-ai/docs/pipelines

Vertex AI Experiments
  https://cloud.google.com/vertex-ai/docs/experiments

KFP SDK v2 コンポーネント設計
  https://www.kubeflow.org/docs/components/pipelines/v2/
```