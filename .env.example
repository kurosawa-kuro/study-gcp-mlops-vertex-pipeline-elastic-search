# Discord通知
DISCORD_WEBHOOK_URL=

# Cloud Run Job（Terraform経由で設定済み。ローカル実行時のみ必要）
GCS_BUCKET=mlops-dev-a-data
JOB_NAME=ml-batch

# MLハイパーパラメータ（省略時はデフォルト値を使用）
# N_ESTIMATORS=100
# MAX_DEPTH=10
# RANDOM_STATE=42
# TEST_SIZE=0.2

# MLflow
# MLFLOW_EXPERIMENT_NAME=california-housing
